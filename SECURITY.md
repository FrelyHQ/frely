# Security

This repository is published for source review. Do not place real API keys, provider credentials, passwords, payment secrets, OAuth tokens, request bodies or production endpoints in issues, pull requests or test fixtures.

The snapshot intentionally excludes production operations and private data. Report a suspected problem through the repository's private security-reporting channel before public disclosure.
